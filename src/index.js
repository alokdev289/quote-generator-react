import React from "react";
import  ReactDOM  from "react-dom";

import App from './App';

const container = document.getElementById('root');

const db = ReactDOM.createRoot(container);
db.render(<App />)